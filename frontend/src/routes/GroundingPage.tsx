/* Universal grounding panel (FR-15).
   Vector stats, graph counts, force-graph visualisation and dynamic upload land
   in Stage 2. The route exists now so the shell is navigable end to end. */

export function GroundingPage() {
  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-lg font-semibold tracking-tight text-text">Grounding</h1>
        <p className="mt-1 text-sm text-text-muted">
          Vector embedding stats, knowledge-graph counts, the graph visualisation and document
          upload land in Stage 2.
        </p>
      </div>

      <section className="card p-8 text-center">
        <p className="text-sm text-text-subtle">Not yet built.</p>
      </section>
    </div>
  )
}
